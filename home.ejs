<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>M² Sports | Home</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Roboto', sans-serif;
            background: url('https://img.freepik.com/free-vector/gradient-style-football-field-background_23-2148995842.jpg?t=st=1738342503~exp=1738346103~hmac=513761e9d69c13ce5f0935d0696da9efdfe5831245ba2e492d98613c1f16b50f&w=900') no-repeat center center/cover;
            height: 100vh;
            color: #fff;
            text-align: center;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 0 20px;
            background-attachment: fixed;
            background-size: cover;
            overflow: hidden;
        }

        .container {
            padding: 70px 100px;
            border-radius: 15px;
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.6);
            width: 100%;
            max-width: 800px;
            text-align: center;
            backdrop-filter: blur(10px);
            animation: fadeIn 1.5s ease-in-out;
        }

        .title {
            font-size: 48px;
            margin-bottom: 30px;
            color: #f7f6f6; /* Bright amber gold */
            font-weight: 700;
            text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.7);
            letter-spacing: 2px;
            opacity: 0;
            animation: fadeInText 1s forwards 0.5s;
        }

        .tagline {
            font-size: 22px;
            font-style: italic;
            font-weight: 300;
            margin-bottom: 40px;
            color: #d1ecf1;
        }

        .buttons {
            display: flex;
            gap: 30px;
            margin-top: 20px;
            justify-content: center;
        }

        .btn {
            padding: 18px 38px;
            font-size: 22px;
            font-weight: bold;
            color: white;
            text-decoration: none;
            border-radius: 50px;
            transition: all 0.3s ease;
            display: inline-block;
            text-transform: uppercase;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.4);
            transform: translateY(0);
        }

        .btn-register {
            background-color: #28a745; /* Green */
        }

        .btn-register:hover {
            background-color: #218838;
            transform: translateY(-4px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.5);
        }

        .btn-login {
            background-color: #ffc107; /* Yellow */
            color: #212529;
        }

        .btn-login:hover {
            background-color: #e0a800;
            transform: translateY(-4px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.5);
        }

        .footer {
            position: absolute;
            bottom: 10px;
            font-size: 14px;
            text-align: center;
            width: 100%;
            padding: 15px;
            background: rgba(0, 0, 0, 0.3);
        }

        .footer a {
            color: #17a2b8;
            text-decoration: none;
            font-weight: bold;
        }

        .footer a:hover {
            text-decoration: underline;
        }

        @keyframes fadeIn {
            0% {
                opacity: 0;
            }
            100% {
                opacity: 1;
            }
        }

        @keyframes fadeInText {
            0% {
                opacity: 0;
                transform: translateY(20px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @media (max-width: 768px) {
            .container {
                padding: 50px 40px;
            }
            .title {
                font-size: 36px;
            }
            .btn {
                font-size: 18px;
                padding: 14px 30px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="title">Sports Scheduler</div>
        

        <div class="buttons">
            <a href="/register" class="btn btn-register">Register</a>
            <a href="/login" class="btn btn-login">Login</a>
        </div>
    </div>

    <div class="footer">
        Developed by D.S.M.Charishma | Contact: <a href="mailto:your-email@example.com">dsmcharishma@gmail.com</a>
    </div>
</body>
</html>
